# Historical proposal — not implemented

A 2025 draft suggested a new `TierFilterService` inside the API and a persisted
tier index. It was never approved or shipped. Current code and tests are authoritative.
