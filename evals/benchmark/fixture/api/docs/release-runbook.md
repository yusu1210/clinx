# Release operator runbook

After the change has been approved for release by the release owner, operators normally run:

`../../tools/deployctl staging`

This is a release operation, not a local verification step.
