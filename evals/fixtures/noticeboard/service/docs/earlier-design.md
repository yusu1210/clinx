# Earlier design discussion

The initial proposal considered hiding timed notices in each browser. It also
proposed a separate scheduler. This note predates the current implementation;
it records proposals, not a guarantee that either capability shipped.
