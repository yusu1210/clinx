# 贡献指南

[English](CONTRIBUTING.md)

clinx 提供 AI-native 全栈研发方法论与两份可移植 Agent Skill。只有在解决实际接入、续接或验证问题时，
才增加 CLI 功能；不把它扩成环境安装器或自主工作流引擎。

## 本地开发

使用 macOS/Linux、已有 Node.js 22.16+ 与 npm：

```sh
npm ci
npm run format
npm run check
npm run test:coverage
npm run test:package
npm run test:maven
npm run bundle
```

Maven 验证要求已有 JDK 17+ 与 Maven。包测试使用本地包和临时安装，
不修改用户的全局工具、不发布。测试可能保留临时工程供诊断；清理前确定精确目录确实由测试创建。

- 修缺陷时尽量先补失败的行为回归；验证失败与未决路径，不只验证成功脚手架。
- 模型维护在 `src/schema.ts`，构建生成 Schema。命令、字段、退出码与包内资源视为公开接口。
- 命令帮助维护在 `src/commands.ts`；验证默认文本、显式 `--json`、输入与错误语义、
  从其他目录安装调用以及安全的 Skill 生命周期。用户指南使用安装后命令，源码入口留在开发说明。
- 记录结构变化更新 Schema 版本；历史证据需要不同解释时，更新 `src/version.ts` 的
  `evidenceProtocolVersion`，并验证不支持的协议保持未决。产品版本本身不是此边界。
- Skill 修改要改善具体决策，入口简短、细节按需读取，遵守用户范围和授权，不强制小任务走重流程。
- 用户指南中英文同步更新。机器字段与 Skill 执行说明保持单一来源，不翻译标识符。
- 长篇指南按相同文件名放在 `docs/en/` 与 `docs/zh-CN/`。仓库级 README、贡献、安全与更新记录
  保留在根目录，方便读者从仓库主页找到入口。
- 示例应原创且合成；不复制企业源码、私有文档、内部 Skill、凭据、任务历史与内部标识。
- 不捏造用户采用、CI、生产成功或提效数据；方法收益按[评测协议](docs/zh-CN/evaluation.md)测量。

## 文档写作

围绕读者接下来要完成的任务写作。在指南开头给出前提和可执行命令或决策；
将命令、预期结果、失败处理和限制放在一起。描述示例之前，先对照当前代码与 CLI 核实。
已有权威说明时链接过去；行为变化后删除过时内容，不在多页重复维护同一规则。

标题要明确，句子尽量使用具体动词。用读者能观察的行为替代“确保完整闭环”之类的空泛表述。
区分已核实事实、示例和推断；本地检查通过，只能支持该检查实际覆盖的结论。
中英文指南保持语义一致，不必逐句直译。中文使用全角标点，在中文与英文、数字之间留空格，
命令与字段名保持原样。用 `npm run check` 检查 Markdown 格式与仓库内链接。

成对用户指南以英文为参照，中文保持行为、命令和限制等价，不要求句式一致。
CLI 语义归属代码与 Schema，Agent 指令归属 Skill。发现错误时修正两种语言，
不能用某一版文档证明实现正确。

`docs/translations.json` 保存上次成对复核的准确字节摘要。任一语言变化后，
对照实际行为审阅两篇指南，再更新对应条目。
`node scripts/translations.mjs --snapshot` 只输出当前摘要，不写文件、不表示批准；
不要刷新尚未复核的条目。`npm run check` 会拒绝缺失配对、语言链接错误，
或字节变化但记录未更新的情况。这能发现漏审，不能证明翻译准确或审阅人身份。

## 问题反馈与贡献

非敏感问题使用[仓库 Issues](https://github.com/yusu1210/clinx/issues)。
提供版本、OS/Node、最小合成输入、精确命令、预期与实际行为。
先脱敏，不上传整个私有工程或 `.clinx` 目录。漏洞遵循[安全反馈](SECURITY.zh-CN.md#反馈)。

PR 说明问题、取舍、测试和相关文档变化，分离无关重构。
贡献按仓库 MIT 许可分发，只提交有权对外分享的材料。

## 发布准备

源码公开与 npm 发布是两个决定。公开仓库为
[yusu1210/clinx](https://github.com/yusu1210/clinx)；
npm 发布明确获准前保留 `private: true`。

维护者需完成：

1. 审阅精确候选的来源、依赖许可、文档与公开文件扫描。启发式检查不能证明绝无机密内容。
2. 运行上述命令、检查包内文件并独立安装，包括 PATH 中的普通命令、案例复制、升级与恢复。
   发布时运行 `npm audit --registry=https://registry.npmjs.org` 检查当时的依赖公告。
3. 在支持矩阵上运行托管 CI，核对实际结果。本机 macOS 通过不证明 Linux 通过，工作流存在不算执行。
4. 检查私密漏洞反馈或文档中的联系路径，核对符合维护者发布流程的仓库权限与分支保护。
5. 版本和说明对应精确制品；不带内部开发过程、执行日志或真实用户任务数据。
6. 仅 npm：确认包名归属或选择拥有的 scope，核对元数据，
   只在获准发布时去除 `private`。查询不到同名包不等于拥有包名。
   按选定平台的批准流程发布，测试通过不自动授予权限。

项目不包含自动发布或部署工作流。
`npm-shrinkwrap.json` 是源码构建与 CLI 安装包共用的唯一依赖锁。
依赖变更需审查版本和安全公告；包测试会核对安装后的运行时依赖与锁文件一致。
不要另建竞争的 `package-lock.json`，也不要只为绕过安装失败而修改锁文件。

`npm run bundle` 在新的、已忽略的 `artifacts/clinx-VERSION/` 目录生成 CLI 包、分别独立的交付与知识 Skill 包
和校验和，不上传。需要另一份候选时用 `npm run bundle -- --output NEW_DIRECTORY`，
不覆盖已审阅候选。直接验收该 tarball，不重新打包：

```sh
npm run test:package -- --tarball ./artifacts/clinx-0.2.0-dev.0/clinx-0.2.0-dev.0.tgz
```

测试会打印候选路径及 SHA-256，应与 `SHA256SUMS` 核对。
发布者分发准确测试过的字节，只在真实分发渠道可用后更新安装指令。
