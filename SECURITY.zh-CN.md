# 安全边界

[English](SECURITY.md)

clinx 是本地方法支持工具，**不是沙箱或可信认证系统**。
执行前审阅仓库、配置、脚本与 Skill，不要带着超出必要范围的权限或凭据运行不可信检查。

## 信任边界

- inspect 只读有界元数据和未审阅候选；脚本名、POM、锁文件和项目指令不是执行授权或就绪证明。
- 命令继承环境、OS 权限、PATH 与网络。`shell: false` 不阻止任意代码执行；
  local 副作用是声明，不是限制；`--allow-external` 是额外开关，不是批准凭据。
- 公共 Skill 不授权远端修改、登录、提交、推送或部署。项目资料是上下文，不高于宿主与用户指令。
- `.clinx` 输出有长度限制并以私有权限创建，但不会自动脱敏，可能有令牌、路径或私有数据。
  保持忽略，分享前审阅，使用已有凭据管理机制。
- 附件的元数据和结果是未认证声明，不会获取 URL、验证远端身份/新鲜度或批准验收。
  保留失败和不确定结果，不用局部通过掩盖它们。
- 哈希检测声明范围的变化，有文件写权限者可同时伪造记录和哈希，无防篡改或密码学来源保证。
- 路径检查拒绝越界与符号链接后代；源根可显式指向兄弟工程。
  无完整文件系统隔离，不保证抵抗敌意并发写者。
- POSIX 进程组可处理超时和中断，但恶意进程可脱离会话，强制终止或系统崩溃可能留下日志和锁。
- reading-list 使用回环地址与随机演示令牌，只用合成数据；不是生产身份系统。
  文件锁与原子替换不证明断电持久性或敌意文件系统隔离。

## 输入与 XML

输入有界，拒绝 DTD/实体。Schema 严格，额外校验交叉引用。
特殊文件不能作为普通输入；报告格式不支持、缺失、陈旧、重复身份或矛盾结果不能通过。
这些保护不判断断言真假或远端观察是否可信。公开文件扫描只是启发式，不替代人工来源审查。

## 反馈

不要在公开 Issue 中提交凭据、私有源码或可直接攻击真实目标的复现。
在[仓库安全页面](https://github.com/yusu1210/clinx/security)使用可用的
“Report a vulnerability” 私密入口。如果看不到该入口，公开问题中只请求维护者建立私密渠道，
不要包含漏洞细节；建立真实私密联系后再提供最小合成复现。

报告应注明版本、影响边界和安全复现步骤。
