# Notice response

GET /notices returns `{ items, total }`. Only active notices are visible; total
counts those same items. The policy source owns visibility. This local demonstrator
has no authentication, persistence or deployment support.
