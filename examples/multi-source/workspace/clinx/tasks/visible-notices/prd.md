# Visible notices

The existing consumer must receive only active notices, with a total that matches
the visible items. Reuse the policy source's visibility rule. Verify the consumer
through the actual local HTTP service. Analytics is unrelated. Do not deploy.
