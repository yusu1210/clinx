# 更新记录

[English](CHANGELOG.md)

## 未发布

- 连续知识练习在隔离检查点间保留实际指南，检测受保护输入变更，覆盖新增消费者、来源不可用、
  配置变化、历史重新出现与后续实现。知识规则区分支持证据与复核范围，保留原有归属并修正检索入口。
- 任务摘要和修订历史支持分页，损坏记录独立报告；状态命令直接读取指定任务并保留字段诊断。
  空白选项和忽略大小写后冲突的检查 ID 在执行前拒绝。
- 状态文本直接显示 Skill 差异与已存阻碍。当前配置或约定缺失、无效时仍可检查附件历史，
  不据此推断当前适用性或允许采集新记录。
- 评测采用评估方持有的源码基线、有期限的评分进程、显式语义审查和计划运行分母。
  隔离 CLI 运行时包含锁定的生产依赖，准备过程不删除已有输出目录。
- 共用工程上下文解析规则：复用已知业务/工程身份、本地绑定和任务范围，处理缺失入口、迁移与副本歧义。
  七种原创上下文场景提供不含工程路径的请求，供独立验收。
- 脚本入口判断解析文件系统路径别名，避免发行包案例与检查经符号链接路径启动时静默跳过执行。
- 默认从 Skill 开始，可运行教程与可选 CLI 记录细节分开；Agent 复用已有接入，按需准备技术记录。
- 命令帮助分组、精简安装输出及只读 `status [ID]` 诊断；有界子目录提示便于调查未配置的多工程目录。
- 独立工程知识 Skill，与交付共用来源、检索、冲突与纠错规则，并说明可选代码智能的使用边界。
- 两份 Skill 均具备安装归属保护，分别提供独立发行包；原创知识案例分开验证沉淀、新会话检索和维护。

- 标准安装命令接入、命令级帮助、默认可读输出与显式 `--json` 自动化接口，
  支持有界管道 JSON 输入和可操作的错误提示。
- Skill 文件归属记录、安全更新、可恢复移除与冲突保护。
- 无需检出源码的内置案例复制、安装资源定位，以及带校验和的本地 CLI/Skill 发行包；
  不代表已发布到注册表或发布平台。
- 可分发依赖锁；案例复制保留包装脚本可执行权限，过滤嵌套本地状态，
  并提供有界遍历与安装包私密文件检查。
- 多源协调：按任务绑定输入、引用源文件、按范围导航，以及可运行的跨仓 HTTP 示例。
- 多源指纹采用有界顺序扫描：最多 64 个声明源，并同时限制单源与单任务条目数和字节数，
  避免并发放大资源占用。
- Skill 安装状态归入本地 `.clinx/install/`；补充宿主访问指引，明确区分声明源与实际文件权限。
- 工作区归属指南与从 PRD 到交付的完整教程，包含可执行的 Skill/CLI 步骤。
  源工程不必补齐统一文档骨架。
- 中英文指南分别位于 `docs/en/` 与 `docs/zh-CN/`，提供成对复核检查；
  可选接入模板位于 `templates/workspace/`。
- 自包含的源与检查定义快照，以及不依赖区域设置的输入摘要。
  当前输入不可用或引用已移除时仍可读取历史，但不能据此支持当前验收。
- 源文件观察附件保留原始归属；回执路径支持显式工作区别名，仍拒绝内部符号链接和越界。
- 静态调查在单个检查目录不可用时保留其他有效候选。JSON 解析错误指出文件但不回显内容；
  标准输出和错误输出分别有界累积。
- 评估对照覆盖时间精度、日期有效性、接收语义、有界重试和结果未知处理，
  不作为 Agent 效果测量结论。

回执、检查点和附件采用记录格式 2、证据协议 2；配置和任务采用格式 1。
不支持的记录需使用对应读取器或重新采集证据，不能直接修改版本字段。
本开发版本尚未发布。

## 0.1.0 — 2026-09-13

首次源码发布，使用已审阅的源码或本地包安装。

- 不绑定目标工程语言与平台的方法论和可移植的 `clinx-delivery` Skill。
- 从 PRD 与工程开始，支持能力复用、真实反馈、协作授权与按目标交付。
- 可选 CLI：静态调查、非覆盖式接入、任务约定、修订、检查点、验证和本地观察附件。
- 显式输入绑定与严格结果解释；附件中的本地通过声明不能批准外部验收条件。
- 中英文文档、可运行的原创合成示例与受控评测夹具。
